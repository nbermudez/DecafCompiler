class GreatestCommonDivisor
{
	int aaaaa = 10;
	int bbbbb1 = 20;
	int x, y, z;

	boolean t = true;

	int array[10];
	
	void main()
	{
		//x = a;
		//y = b;
		z = gcd(x, y);
		z = gcd(x, y);
		z = gcd(x, y);
		z = gcd(x, y);
		z = gcd(x, y);
		z = gcd(x, y);

		print z;
		print "hola mundo \n";
	}

	// Funcion que calcula el maximo comun divisor
	int gcd(int a, int b)
	{
		if (b == 0) {
			return (a);
		} else {
			return (gcd(b, a % b));
		}
		print "hola mundo \n";
	}
}
